# Vision-alignment checks

Standing workflow (owner request, 2026-06-02): after publishing each implementation
prompt, run the `vision-alignment-reviewer` to sanity-check it against the product
vision — `../PlantApp/ChatHistory.md` (the owner's original brief) — and record the
verdict here. Revise or escalate to the owner on drift before the implementation
proceeds. Encoded in `CLAUDE.md` (step 6), `.claude/rules/prompt-contract.md`, and the
`implementation-prompt-writer` skill.

| Handoff | Date | Verdict | Note |
|---|---|---|---|
| `0006-api-add-plant` | 2026-06-02 | **ALIGNED** (ship as-is) | All 8 principles ok: GardenSpace+Container+PlantInstance first-class; deterministic engine **imported, not reimplemented**; data-driven profile loaded at runtime; backend-only, no keys; no premature AI/photos/space-optimizer; Fastify recorded as a new ADR (D-01 left the web framework unpinned). No scope drift. |
| `0007-api-read-delete` | 2026-06-02 | **ALIGNED** (ship as-is) | #19 RLS isolation + #20 cascade delete — both explicit Slice 1 plan items (impl-plan #19/#20); `GET/DELETE /plants` in plan backend scope; RLS test *strengthens* privacy; care-engine/migrations forbidden; no AI/photos/space-optimizer pulled in. No drift. |
| `0008-lint-config` | 2026-06-02 | **N/A (tooling chore)** | ESLint↔tsconfig config fix only — no product surface, no entity/feature/scope change, touches no `src/`/`care-engine/`/schema/migration logic. Vision-alignment has no dimension to assess; gate recorded N/A by the planner rather than spinning a subagent. |
| `0009-android-wrapper-build` | 2026-06-02 | **N/A (tooling chore)** | Generate the Gradle wrapper + assemble the existing 6-module skeleton; no Compose/feature code, no new deps (uses the committed `libs.versions.toml`, which already excludes CameraX/FCM/AI), no product surface. The real Android vision-check lands on a2 (Compose screens/tests). |
| `0010-api-contract-conformance` | 2026-06-02 | **ALIGNED** (planner-assessed) | *Strengthens* alignment: conforms API responses to the camelCase `shared-schemas/*` — the documented cross-boundary contract (principle: structured outputs / schemas shared by Android+backend+AI; D-06). No new product surface/scope/deps; adds Ajv response-validation tests. Conformance-to-existing-contract, so recorded by planner without spinning the subagent. |
| `0011-android-network` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Android `:network` DTOs + Retrofit client. Confirms D-02 (Retrofit/OkHttp/kotlinx, not Ktor), D-06 (networknt in tests only; handwritten `@Serializable` DTOs), D-09 (no Android care-engine — `CareTaskDto` is a read-only carrier), D-11/D-12 (no CameraX/GPS), no FCM/WorkManager/AI deps, no Compose yet, camelCase matching the conformant API. No drift. |
| `0012-android-domain-data` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | `:domain`/`:data` repository over `:network`. D-09 honored (`CareTask` opaque, no on-device care logic); no Compose/Room/forbidden deps; DataStore for base URL/token (no secrets); MockK test-only. **Room-defer assessed a reasonable Slice-1 boundary** (Slice 1 reads live; roadmap puts offline/Room with Slice 3+ reminders). No drift. |
| `0013-android-inventory-ui` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Slice 1 Compose UI (add/list/detail) + UI tests #21–#24. Form is profile+container+gardenSpace (not flat species); detail renders CareTask opaque (D-09); no camera/photos/GPS/notifications/AI/space-planner (D-11/D-12); Robolectric (no silent emulator); #21–#24 map to plan. **Minor (not drift):** plan's *optional* `nickname`/`placement` form fields not named in the prompt — reviewer said ship-as-is / note for retro. **Action:** verify form on landing; tiny follow-up only if dropped & wanted. |
| `0014-slice2-foundation` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Slice 2 opener: plan doc + `advisory.schema.json` contract + red→green schema test. All 5 `@slice-2` BDD scenarios represented; advisories deterministic + profile-driven + **never auto-create CareTasks** (invariant) + **no AI**; no premature slices (no feeding/weather/AI/notifications); seed ideal-range gap deferred to the engine step. No drift. |
| `0015-advisory-engine` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Deterministic `computeAdvisories` engine. Rules field-driven (no species `if` branches); severities match BDD (container-size "high"); returns `Advisory[]` only (no CareTask — invariant); no AI; tests dynamic-import + schema-validate output vs `advisory.schema.json`. **2 minor (non-blocking):** (a) test fixtures must use uuid `plant.id` or schema-validation goes red — red-first self-corrects; (b) slice-02 plan's "seed gap → S2.1" line is stale (enrichment moved to S2.2 — fix in S2.2). No drift. |
| `0016-advisories-api` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | `GET /plants/:id/advisories` (calls `computeAdvisories`, RLS-scoped, **persists nothing / no CareTask**) + migration `0004` ideal-range (additive jsonb merge) + seed enrichment + slice-02 plan-line fix. Integration tests map all 5 `@slice-2` scenarios; responses Ajv-validated; passion fruit 95/190; RLS 404; no AI/species-branches/premature features. **Minor (low-risk):** the no-CareTask invariant is enforced architecturally (GET creates nothing), not via an explicit "no `care_tasks` row" DB assertion — optional follow-up. No drift. |
| `0017-android-advisories` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Android advisory display: `:network` `AdvisoryDto` + `getAdvisories` (Retrofit/kotlinx, D-02), `:data` mapping, `:feature-inventory` detail renders advisories (severity-styled) + Compose UI test; `:network` test schema-validates the DTO (networknt, D-06). **Informational-only** — no on-device computation, **no advisory→task action** (invariant), no AI, no forbidden deps, no backend/schema change. No drift. |
| `0018-validate-schemas-fix` | 2026-06-02 | **N/A (tooling chore)** | Make `npm run validate-schemas` green: add `-c ajv-formats` to the ajv-cli script + one `type:"array"` in `diagnosis-result.schema.json` (strictTypes). No product surface / behavior change; not a feature. |
| `0019-list-endpoints` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Backlog 3a: read-only `GET /plant-profiles` (global catalog), `GET /garden-spaces`, `GET /containers` (RLS-scoped) + new `toPlantProfile` mapper + integration tests, feeding the add-plant **selectors** (3b). Serves the data-driven inventory vision exactly; reuses existing schemas (camelCase contract). No AI/photos/GPS/notifications, no species if-branches, no write path — create nothing (task/advisory invariant structurally untouched). RLS-isolation test *strengthens* privacy. One cohesive change. No drift. |
| `0020-android-network-lists` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Backlog 3b part 1/3 (network): Android `:network` `PlantProfileDto` (nested profiles as `JsonObject`, scalars typed) + `getPlantProfiles/getGardenSpaces/getContainers` Retrofit calls + networknt schema-validation test/fixture (D-06). Read-only data carrier — D-09 honored (no Android care logic), no AI/photos/GPS/FCM, no write/auto-create. Network→data→UI split is correct vertical-slice discipline. No drift. **BLOCKED at gate** (Android SDK on unmounted Drive); superseded by `0021` re-run after owner remounted. |
| `0021-android-network-lists-rerun` | 2026-06-02 | **ALIGNED** (inherits `0020`; no scope change) | Re-run of `0020` after the owner re-mounted the external Drive (SDK resolves). Identical `:network`-only scope (commits the already-written `PlantProfileDto` + 3 GET calls + schema test once the gate runs green). No product-surface change vs the agent-reviewed `0020`, so recorded by the planner without re-spinning the subagent. No drift. |
| `0022-android-data-lists` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Backlog 3b part 2/3 (data/domain): lean `:domain` `PlantProfile` (id/scientificName/commonNames/category — **no** care profiles, display/selector carrier only → D-09 honored) + 3 read methods on `InventoryRepository`; `:data` `PlantProfileDto.toDomain()` (drops nested jsonb) + impl methods + `FakePlantAppApi` update + mapping test. Serves the data-driven inventory vision; read-only (creates nothing). Also fixes the latent `:data` compile-red left by `0021` (red→green). One cohesive change. No drift. |
| `0023-android-profile-dropdown` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Backlog 3b part 3/3 (UI), sub-step a: replace the add-plant **Profile id text field** with a Material3 catalog-driven **dropdown** (`getPlantProfiles()`), `AddPlantViewModel` loads the catalog, one-line `:app` route wiring, updated Robolectric `#22`/`#24` + new dropdown test. Directly implements the vision's data-driven plant-profile selector (don't hard-code species). No AI/photos/GPS/notifications, no on-device care logic (D-09), no species if-branches; selection creates nothing (submit path unchanged; `AddPlantForm` unchanged). Space/container select-or-create correctly deferred to 3b-ui-b. No drift. |
| `0024-android-gardenspace-selector` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Backlog 3b-ui sub-step b: replace the **Garden space id** field with a **select-or-create** control (dropdown from `getGardenSpaces()` + inline create via existing `createGardenSpace(name, kind)`), VM load/create + `:app` wiring + Robolectric tests. Serves the garden-map first-class-entity vision. **Privacy confirmed:** create form is **name+kind only** — no postalCode/hardinessZone/location collected (matches ZIP-default, no-continuous-GPS stance). No AI/photos/GPS/notifications, no on-device care logic (D-09); create is an explicit user action via an existing endpoint — no task/advisory auto-creation. Container deferred to 3b-ui-c. No drift. |
| `0025-android-container-selector` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Backlog 3b-ui sub-step c (final selector): replace the **Container id** field with **select-or-create** (dropdown from `getContainers()` + inline create via existing `createContainer(name, volumeLiters, material, drainage)`); validation moves onto the selection. Container size/material/drainage are first-class care-engine inputs — core to the vision. Structurally identical to ALIGNED `0024`. Create form is volume/material/drainage/name only — no location/photos. Care task still computed backend-side at add-plant (not here); explicit-user create via existing endpoint, no auto-task. After this, **3b complete — add-plant fully selector-driven, no raw-id fields.** No drift. |
| `0026-android-auth-network` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Backlog 3c part 1/3 (network), **owner chose email-OTP flow**: `:network` `SupabaseAuthApi` (GoTrue `/auth/v1/otp`+`/auth/v1/verify`) + DTOs (`@SerialName` snake_case) + factory (public anon `apikey` header, caller-supplied auth URL). Required infra for the multi-user/RLS vision (real JWT the backend forwards to RLS). **Privacy/secrets safe:** BASIC logging (no bodies → no email/OTP/token in logs), nothing hard-coded (anon key is public, caller-supplied). No AI/photos/GPS/notifications; computes/creates no care data (invariants untouched). Data+UI deferred. One cohesive change. No drift. |
| `0027-android-auth-data` | 2026-06-02 | **ALIGNED-WITH-NOTES** (agent-reviewed; ship as-is) | Backlog 3c part 2/3 (data/domain): `:domain` `AuthRepository` (requestOtp/verifyOtp) + `:data` impl over `SupabaseAuthApi` persisting the token via `SettingsStore.setToken` (`TokenWriter` seam) + DI/config + fake tests. Closes the real per-user-JWT loop the RLS vision needs. **Secrets check PASSES:** commits only the **public** local-dev anon key (read live from `npx supabase status`, overridable); **service_role never referenced**; STOP-guard if stack down. Token → DataStore only, not logged. No AI/photos/GPS/notifications; creates no care data. **Note (already satisfied):** the test asserts `VerifyOtpRequest.type=="email"` — `0026` landed that DTO with `type = "email"` default, so consistent. No drift. _(Landed `28f69ea`; verified committed key decodes role=anon/iss=supabase-demo — not service_role.)_ |
| `0028-android-signin-ui` | 2026-06-02 | **ALIGNED-WITH-NOTES** (agent-reviewed; ship as-is) | Backlog 3c part 3/3 (UI): stateless `SignInScreen` (email→send code→verify) + `SignInViewModel` over `AuthRepository` + `:app` gating (`tokenBlocking()` → start destination; verify→navigate to list) + Robolectric tests. Serves the multi-user/RLS sign-in need; **email-OTP only (no password/social)** — privacy-minimal, consistent with the consent stance. No AI/photos/GPS/notifications; email/OTP transient (not logged/persisted beyond the DataStore token); creates no care data. One cohesive change. **Non-blocking structural-debt note:** sign-in lives in `:feature-inventory` for MVP pragmatism; should migrate to a dedicated `:feature-auth`/`:feature-settings` module before later phases (tracked in Deferrals). No drift. _(Landed `e76ff8d`.)_ |
| `0029-care-engine-task-from-advisory` | 2026-06-02 | **ALIGNED-WITH-NOTES** (agent-reviewed; ship as-is) | Backlog 3d part 1/N (engine): **pure deterministic** `computeTaskFromAdvisory` (no I/O/`Date.now`/random; `canonicalJson`+`sha256` like the water engine; output validates vs `care-task.schema.json`). **Both invariants hold:** persists nothing & not wired to any endpoint → GET-advisories still creates nothing (no auto-create); deterministic core, no AI. **Mapping is vision-faithful, NOT invented** (per ChatHistory: container restriction→larger container=`repot`; support/trellis=`support`; pollination="grow another plant nearby" = multi-plant recommendation, not a single task → throws/unsupported→3d-api 400). This entry is the **recorded design decision** (mapping + severity→priority low/normal/high + dueAt=clockUtc) that 3d-api/Android inherit. Notes (non-blocking): `wateringBaselineAt=clockUtc` is a schema-required placeholder for non-water tasks (mild semantic debt); `inputsHash` intentionally hashes `{kind,sourceInputs}`. No drift. _(Landed `e4ffe4b`; verified pure, `npm test` 67→72.)_ |
| `0030-api-advisory-accept` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Backlog 3d part 2/N (API): `POST /plants/:id/advisories/accept {kind}` — recompute advisories (RLS 404 if not owned), match the **currently-applicable** advisory (400 if absent), call the pure `computeTaskFromAdvisory` (400 if it throws, e.g. pollination), persist **one** `care_tasks` row, return the CareTask. **Invariant preserved:** task created ONLY on explicit user POST via the deterministic engine; the GET path stays create-nothing (Forbidden-list protected + a DB-observable test asserts tasks increase by exactly 1 only after the POST — closes the optional follow-up from `0016`). RLS-scoped (user B → 404); no AI/photos/GPS/notifications. Reviewer verified all columns/RLS against the real repo. One endpoint, one commit; Android deferred. No drift. _(Landed `53d093e`; `test:int` 31→35; verified GET handler has no insert.)_ |
| `0031-android-accept-netdata` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Backlog 3d part 3 (Android net+data): `:network` `acceptAdvisory` + `AcceptAdvisoryRequest` DTO, `:domain` port method, `:data` impl (api→`toDomain`) + `FakePlantAppApi` update + tests. **D-09 honored** — Android holds no care logic; `AcceptAdvisoryRequest` carries only `kind`, return is the opaque server-computed `CareTaskDto`→`CareTask`; no on-device task computation. No-auto-create intact (a task is created server-side only via the explicit accept POST, user-initiated in 3d-ui). Net+data combined in one commit for the sound `0021→0022` compile-break reason. No AI/photos/GPS/notifications/Room. UI deferred. No drift. _(Landed `bfdd946`; `:network` 16→17, `:data` 10→11.)_ |
| `0032-android-accept-ui` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Backlog 3d final (UI): per-advisory **Accept** button on `PlantDetailScreen` (shown only for `container-size`/`support`, **not** `pollination` — matches the engine/API) → `PlantDetailViewModel.accept` → `repository.acceptAdvisory` → reload + `:app` route wiring + Robolectric tests. **Invariant at UI layer:** a CareTask is created ONLY by the explicit user tap → accept endpoint; GET-advisories display still creates nothing. D-09: VM just calls backend + reloads, no on-device care computation. No AI/photos/GPS/notifications. One cohesive change. **Completes backlog (3) UX follow-ups** (add-plant selector-driven + email-OTP sign-in/gating + advisory→accept→CareTask e2e). No drift. _(Landed `d1bda81`; `:feature-inventory` 14→16.)_ |
| `0033-navhost-smoke` | 2026-06-02 | **ALIGNED-WITH-NOTES** (agent-reviewed; ship as-is) | Backlog (2), **owner chose Robolectric NavHost smoke**: test-only `:feature-inventory` Robolectric test driving a mirrored `NavController`/`NavHost` over the real screens + ViewModels built from **fake** repos (no Hilt-test/emulator/backend) — gated journey sign-in→list→detail→accept (+ add). Strengthens confidence in the already-built flows; **no `src/main` change** (Forbidden-list STOPs if a seam is needed); adds only `navigation-compose` testImpl. D-09 safe (fakes; no on-device care logic); no-auto-create untouched (fakes record, don't persist); no AI/photos/GPS/notifications/secrets. **Note (tracked):** test mirrors (can't import) the `:app` nav graph — guard comment required + retro note; `:app:assembleDebug` compile-checks the real graph. No drift. _(Landed `da020e3`, test-only; `:feature-inventory` 16→18.)_ |
| `0035-workmanager-local-reminders` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) + **no-mutation guardian PASS** | Slice 3 step 2 (WorkManager **local** path): `ReminderWorker` (CoroutineWorker, content via `inputData`, **permission-guarded** post so an ungranted `POST_NOTIFICATIONS` can't crash), `ReminderScheduler` (unique delayed work per `ReminderSpec` from `0034`), + WorkManager dep + `POST_NOTIFICATIONS` declaration + channel; Robolectric `WorkManagerTestInitHelper` scheduling tests. Reviewer cited ChatHistory **lines 1/167-168/175/177/556** — WorkManager + local notifications (before FCM) is exactly the prescribed stack + explicit-consent posture. **D-09 honored** (delivers reminders; `dueAt` from backend-fed `computeReminders`; no on-device care logic, no AI). **FCM STOP gate intact** (Forbidden bans google-services/FCM). Dep+permission minimal & owner-approved (D-13). Runtime-permission UI + app-open wiring correctly deferred. Note: gate asserts scheduling, not real notification post (needs device) — validate post once runtime-perm lands. No drift. |
| `0045-app-backdrop-glass` | 2026-06-02 | **ALIGNED-WITH-NOTES** (vision) + **guardian PASS** | Modern UI overhaul (owner: "whole app needs a background/backdrop + modern color scheme"): app-wide themed **backdrop** (`PlantAppBackground` — theme-aware gradient + radial "sunlight" glow) + translucent **`GlassCard`** in `:design-system` (Codex-generated, planner-reviewed), wrap NavHost (`:app`), transparent Scaffolds + Fraunces-serif app-bar titles + **fix the off-theme lavender wizard tiles** (`:feature-inventory`). Faithful to Verdant Glasshouse (immersive glass surfaces). Presentation-only — no AI/photos/GPS/secrets/backend/engine/data/nav; test tags + behavior preserved; no new dep; no raster (hero leaf photography correctly DEFERRED). One cohesive change across the 3 UI modules; build-green gate. **Verified-on-landing reminder:** report real `assembleDebug` output (the Codex verbatim files must actually compile). No drift. |
| `0044-verdant-glasshouse-theme` | 2026-06-02 | **ALIGNED-WITH-NOTES** (vision) + **guardian PASS (after fixes)** | Modern/thematic UI overhaul, theme foundation (owner picked **Verdant Glasshouse** from Codex-generated directions): build `:design-system` (currently bare default M3) into full light+dark M3 color schemes + **Fraunces/Manrope OFL** typography (bundled TTFs) + modern Shapes + `PlantAppTheme(darkTheme, content)` (no dynamic color) — re-skins the whole app via existing tokens. Code generated by **Codex**, planner-reviewed (path corrected java→kotlin). Serves owner's beautiful/modern ask + the vision's Compose/M3 + design-system layer + "polish" phase; content stays beginner-plain. **Licensing OK** (OFL bundlable, license preserved). Presentation-only; no engine/backend/AI/photos. Guardian initially BLOCKED (missing expected-failure section + a libs.versions.toml/no-dep contradiction); **fixed before publish** (added expected-failure section, scoped git-add to `:design-system/`, FontVariation-weight guidance, font-integrity check) → re-guardian PASS. Signature components + copy sweep deferred. No drift. |
| `0043-wizard-sourced-icons` | 2026-06-02 | **ALIGNED-WITH-NOTES** (beginner-first + license check; guardian PASS) | Owner directive after device review ("don't author your own svg icons — find them online"): replace `0042`'s weak placeholders (the on-device run found **all 6 pots = identical flowerpot**) with **real sourced icons** — species (5) from `openfarmcc/open-crop-icons` (**CC0**, verified at source: tomato/basil/strawberry/tomatillo + generic-plant→passion-fruit) → vector drawables; pots/locations from **Material Symbols** (Apache-2.0) via `material-icons-extended`, **distinct** glyphs (bucket/window-box/raised-bed differ). Licenses verified safe to bundle (CC0 needs no attribution; Apache NOTICE preserved); no emoji/raster/hand-drawn/paid-or-attribution sets. Presentation-only (no engine/backend/schema); reasonable UI dep. **Spec updated (the old "author original vectors" line was superseded).** No drift. |
| `0042-addplant-wizard-screen` | 2026-06-02 | **ALIGNED-WITH-NOTES** (beginner-first; guardian PASS) | Beginner add-plant wizard H2: 3-step `AddPlantWizard` (What growing? → Where? → What's it in?) + confirm, replacing the jargon `AddPlantScreen`; big **custom-vector-icon** + plain-name tiles; **no emoji**, no litres/material/drainage/growth-stage/ISO shown; engine derives values via the `0041` model; original vector drawables per species (+ location/pot) — no licensing risk; emoji `categoryIcon` removed. Faithful to the vision's onboarding wizard / Beginner mode (ChatHistory 563/593). Deterministic engine still owns care; no AI/photos/GPS; reuses existing VM/repo; vectors-only. **Reviewer-flagged hardening folded into the prompt before publish:** (1) reuse-not-duplicate space/container on Back/re-tap, (2) select by created identity (not `list.last()`) + disable Add until ids resolve (the VM create path is async), (3) no emoji literals from the spec in code. No drift. |
| `0041-addplant-wizard-model` | 2026-06-02 | **ALIGNED** (beginner-first lens; repo-verified) + **guardian PASS** | Beginner add-plant wizard H1: pure unit-tested model — pot sizes "how pots are sold" (4-inch/6-inch/1-gal/5-gal/window box/raised bed → litres for the engine), 4 location presets, **data-driven `categoryIcon(category)`** (keys off `PlantProfile.category`, NOT per-species → honors "don't hard-code species"), hidden defaults. Directly realizes the **original vision's onboarding wizard** (ChatHistory 563/593/526: "What plant? Where? What size container?" / Beginner mode / "simple advice, not raw horticulture"). Engine still owns care (gets `volumeLiters` from a friendly choice); pure/no-deps/no-AI; UI deferred to H2. Litre values sane (5-gal=19 matches the engine fixtures). **Note:** category icons mean passion fruit→🍅 (accepted MVP trade-off; per-species icons later, owner OK'd assets). No drift. |
| `0040-android-internet-permission` | 2026-06-02 | **N/A (on-device bugfix) + guardian PASS** | One-line fix: declare `android.permission.INTERNET` in `app/src/main/AndroidManifest.xml`. The real on-device run found the app had **no INTERNET permission** → all sockets `EPERM`; missed by every unit/integration/Robolectric test (none open a real socket). Pure enabling-bugfix, no product surface; the app has always needed INTERNET (it's a backend-only client). Guardian PASS (single-file, single-purpose, aapt2 INTERNET green gate, no deps/destructive git). |
| `0039-android-device-debug-build` | 2026-06-02 | **ALIGNED** (agent-reviewed, repo-verified) + **guardian PASS** | Device-targetable Android config: base URLs → `BuildConfig` (`-P`-overridable; **API corrected from the mis-set Supabase `:54321` to Fastify `:3000`**, auth stays Supabase `:54321`) + a **debug-only** cleartext `network-security-config`. Reviewer verified against the real repo: `/plants…`/advisories/spaces/containers are Fastify `requireAuth` routes, auth is GoTrue `:54321` — split matches ADR-0006 (app→Fastify w/ Supabase JWT→RLS). **Security:** cleartext scoped to `:app/src/debug/` only — **release stays HTTPS-only** (owner chose cleartext for the local LAN test; **prod-HTTPS tracked as a requirement**); **no host IP committed** (LAN `10.0.0.179` via `-P` at build only); no apk committed. Backend-only, no AI/photos/GPS/FCM, no new dep. **Op note:** re-close `ufw` 3000/54321 after testing. No drift. |
| `0038-backend-server-bootstrap` | 2026-06-02 | **N/A (infra/runnability) + no-mutation guardian PASS** | Adds `backend/src/server.ts` (`buildApp().listen({host:'0.0.0.0', port})`) + a `start` script so the existing Fastify app can run over HTTP for the on-device full-stack test. No product surface / route / engine / schema change — purely makes the already-built app runnable (it had only `app.inject()` test usage). Enables, doesn't alter, the backend-only architecture. Vision-check N/A (consistent with the wrapper/lint infra chores); guardian PASS (single-purpose, dummy-env liveness gate, no secrets/deps/destructive git). |
| `0037-post-notifications-permission` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Slice 3 step 4 (last LOCAL step): runtime `POST_NOTIFICATIONS` request (Android 13+) — pure unit-tested `NotificationPermission.shouldRequest(sdkInt, granted)` + a Compose `RequestPermission` launcher in MainActivity's LIST route (request once on 33+ when not granted; no-op below 33). Serves the vision's notification feature with **explicit system-dialog consent** (ChatHistory 1/167-168/177/556). No AI/photos/GPS/background-location, no new data collection, no secrets. **FCM STOP gate intact** (Forbidden bans google-services; planner STOPs for owner Firebase setup after this). Pure helper + one launcher wiring, one commit. No drift. |
| `0036-reminder-sync-appopen` | 2026-06-02 | **ALIGNED** (agent-reviewed; ship as-is) | Slice 3 step 3 (app-open scheduling): `ReminderSync` coordinator (gather pending CareTasks across plants → `computeReminders` → `ReminderScheduler.schedule`) wired fire-and-forget into `PlantListViewModel` on successful load; adds a `ReminderScheduling` seam + injected `Clock` for testability; hand-fake unit test (fixed clock, only the pending task scheduled). **D-09 honored** (orchestrates only; `dueAt` backend-computed; no on-device care logic, no AI). **D-13/local-only** — FCM/google-services Forbidden; runtime `POST_NOTIFICATIONS` UI deferred to next; Worker already no-ops safely without the grant. Privacy: reads only user-owned tasks, no new collection/GPS/photos/secrets. One coordinator + DI + one trigger. No drift. |
| `0034-slice3-opener` | 2026-06-02 | **ALIGNED-WITH-NOTES** (agent-reviewed; ship as-is) | **Slice 3 opener** (backlog 4): `docs/slice-03-reminders-plan.md` + pure deterministic `computeReminders(tasks, now, leadTime, staleAfter)` in `:domain` (java.time, no Android, no `Instant.now` inside) filtering pending CareTasks → local-reminder specs at `dueAt`. **Matches the vision's core** ("notify me when I need to water"; WorkManager-before-push). **D-09 honored** — `dueAt` consumed verbatim from the backend engine; this is on-device *delivery timing*, not care computation. Posture relaxation (D-11/D-12 no-notifications) correctly slice-scoped; this handoff adds **no permission/notification/dep** (deferred). Deterministic, no AI. **FCM STOP gate preserved.** Folded in the reviewer's notes: plan doc records the placement as a **ratified decision (D-13-style)** (local scheduling/delivery on-device, care computation backend, FCM later/owner-gated) + a past-trigger("fire immediately") note + test case. No drift. |

_Earlier handoffs `0001`–`0005` predate this gate; they were aligned by construction
(schema validation, deterministic care-engine, seed catalog, DB foundation — all
core-vision items)._

## 0046 — fix dark-mode content color on transparent Scaffolds (2026-06-02)
- **Guardian:** PASS — all 10 contract sections present; single-purpose; SHA + 3 file/line refs
  verified against live repo; no installs/destructive git; exchange boundary respected.
- **Vision:** ALIGNED — 3-line Compose styling fix on accepted 0045 work; serves beginner-first
  UX (invisible dark-mode text); no engine/AI/privacy/slice-order implications. Ship as-is.

## 0047 — Garden Hearth design tokens (Wave 2 W1 slice 1) (2026-06-10)
- **Guardian:** PASS — all 10 sections; every old value line-verified vs live repo; HEAD a5968a4
  confirmed; gradle cmds = standard impl verification, no new deps.
- **Vision:** ALIGNED — token-only entry slice authorized by PD-08/PD-09; type bump serves
  beginner readability; all values 1:1 with Garden Hearth spec. Ship as-is.

## 0048 — GlassCard → mostly-opaque Hearth surfaces (Wave 2 W1 slice 2) (2026-06-10)
- **Guardian:** PASS — 10/10 sections; HEAD 05fba1c + both old alpha expressions verified live.
- **Vision:** ALIGNED — exact Hearth spec values (0.94/0.90); W1 foundation slice; ship as-is.

## 0049 — bottom-tab navigation shell (Wave 2 W1 slice 3) (2026-06-10)
- **Guardian:** PASS — 10/10; HEAD 7b5ba83, catalog dep + icon imports + no :app tests all verified live.
- **Vision:** ALIGNED — Spaces gets its first-class nav slot (core vision), Assistant correctly
  deferred to W5, placeholder copy beginner-clean. Ship as-is.

## 0050 — beginner-friendly plant detail (Wave 2 W1 slice 4) (2026-06-10)
- **Guardian:** PASS — 10/10; HEAD 130c391; every old string/assertion + kind-enum match verified live.
- **Vision:** ALIGNED — badge removal + rationale de-emphasis is the vision's own Beginner-mode
  (ChatHistory "Expert mode/Beginner mode"); engine data untouched in model/API; ship as-is.

## 0051 — HOTFIX wizard location kinds vs DB constraint (2026-06-10)
- Found by the 0050 per-slice device check (PD-10 paying off): 3/4 location presets 400 on the
  garden_spaces kind constraint → silent permanently-disabled Add. Only Balcony worked.
- **Guardian:** PASS — bug + all 4 file/line refs + constraint list verified live.
- **Vision:** ALIGNED — "Backyard"→other uses an existing sanctioned enum member; labels stay;
  no schema migration in a hotfix slice; follow-up error-surfacing slice queued. Ship as-is.

## 0052 — My Garden refresh-on-visit + quiet refresh (Wave 2 W1 slice 5) (2026-06-10)
- Found by the 0051 device check: new plant invisible until force-stop (restoreState keeps the VM).
- **Guardian:** PASS — HEAD e726070; unconditional-Loading bug + insertion points verified live.
- **Vision:** ALIGNED — bug fix; ReminderSync-on-every-visit consistent with its "on app open"
  intent (fire-and-forget, no engine impact). Ship as-is.

## 0053 — Hearth list rows + friendly names (Wave 2 W1 slice 6) (2026-06-10)
- **Guardian:** PASS — HEAD a222929; current PlantRow/Content/prettify/icon/overload/tag state all verified live.
- **Vision:** ALIGNED — names data-driven via getPlantProfiles (no hard-coding); slug-leak kill =
  beginner-first TOP constraint; 104dp/64dp per Hearth spec. Ship as-is.

## 0054 — wizard error surfacing + natural confirm copy (Wave 2 W1 slice 7) (2026-06-10)
- **Guardian:** PASS — HEAD 1019e19; error-flow gap + all old-state refs verified live; A+B
  accepted as one user story ("wizard talks like a person").
- **Vision:** ALIGNED — plain-language error card = Beginner mode; phrase echoes the picked
  space (reinforces space-first model). Ship as-is.

## 0055 — GoTrue refresh endpoint (:network), token-refresh part 1/3 (2026-06-10)
- Found by the 0054 device check: expired access token = permanent 401 dead end (refresh token
  received but never stored/used; no 401→sign-in path).
- **Guardian:** PASS — HEAD a0cbc3d; endpoint shape sanity-checked vs GoTrue conventions.
- **Vision:** ALIGNED — maintenance on shipped sign-in (0026-pattern decomposition). Ship as-is.

## 0056 — persist refresh token + auto-refresh on 401 (:data), part 2/3 (2026-06-10)
- **Guardian:** PASS — HEAD 1a60c3f; all deltas verified live; clarity note (FakeSupabaseAuthApi
  override) folded into the prompt before publish.
- **Vision:** ALIGNED — session credential ≠ provider key (D-11/D-12 intact); DataStore parity
  with existing access token (encrypted-store hardening = separate future handoff). Ship as-is.

## 0057 — sign-in fallback on SessionExpiredException, part 3/3 (2026-06-10)
- **Guardian:** PASS — every claim verified live at 738fb9c (incl. :domain:test JVM task note).
- **Vision:** ALIGNED — auth hygiene prerequisite for real-world use; no feature pull-forward.

## 0058-signin-hearth-polish (2026-06-11)
- Guardian: PASS (all 10 sections; live spot-checks vs 4b3910c all match; named-args
  safety for the inserted `busy` param confirmed across MainActivity + both test files;
  no other test asserts the changed strings).
- Vision: ALIGNED — W1-remainder sign-in polish per wave2 plan; beginner-first copy
  clean; privacy IMPROVED (raw e.message/LAN-IP leak removed from sign-in UI);
  "6-digit code" grounded in supabase/config.toml otp_length=6; Fraunces via type
  scale only; GlassCard consumed as-is. Ship as-is.

## 0059-friendly-errors (2026-06-11)
- Guardian: PASS — all 6 e.message sites + 2 screen prefixes verified verbatim vs 4517f44;
  red test compiles on baseline (MutablePlantsRepo/reminderSync confirmed); no test pins any
  removed string; wizard error-card test passes "boom" directly (unaffected); SignIn untouched.
- Vision: ALIGNED — exact PD-13 scope; all 7 new strings beginner-clean ("pot"/"space" match
  existing app vocabulary); privacy improved (LAN-IP leak removed everywhere); deferred
  wizard/detail routing judged better-than-status-quo, no escalation. Ship as-is.

## 0060-houseplant-category (2026-06-11)
- Guardian: PASS — PD-14 approval verified for the migration; fixture validated against every
  schema required-list (only the enum fails = clean red); constraint name + fallback adequate;
  no test pins the enum list; db reset = local-only, accepted (device data re-established at
  next sign-in).
- Vision: ALIGNED — exact Gate B ripple, vocabulary-before-seed ordering confirmed correct;
  pothos fixture = test artifact (rolled back), no hallucinated-horticulture violation;
  catalog seeding + picker chips correctly deferred. Ship as-is.

## 0061-baseline-grants (2026-06-11)
- Guardian: PASS — §5a grants word-identical to the 0060 report's manual fix; RLS verified
  enabled on all 5 tables, anon policy-less; deliberate broken-state red judged safe
  (local-only, transient, STOP-if-green guard); counts consistent (36→38).
- Vision: ALIGNED — pure harness repair enabling W2 seed batches; red-first-by-reset is
  objective + reproducible; hosted-parity claim verified against Supabase defaults.
- Privacy (extra gate): PASS — GRANT-only; zero anon-readable rows; no new
  permissions/SDKs/secrets. 0060 deviation accepted as environment repair.

## 0062-catalog-batch1 (2026-06-11)
- Guardian: BLOCK→PASS — first pass caught a REAL data defect (kale leach -7); planner stripped
  13 incoherent optional values across the 75 (PD-16), regenerated SQL; focused re-check PASS
  (16 tuples, ids match, tomato v2 vegetable, no DDL/GRANT, escaping sane, batches 2/4 clean).
- Vision: ALIGNED — cited-data rule verified on 3 spot-checked profiles (UMN/Clemson/RHS/UC ANR
  citations present); tomato fruit→vegetable judged acceptable beginner-first framing; UI work
  correctly deferred; verticalSuitability preserved for W4/W5. Publish-attachment note resolved
  by publishing with the new exchange attachment flow (checksums verified).

## 0063-catalog-batch2 (2026-06-11)
- Guardian: PASS — 20 tuples match BATCH2_IDS exactly; categories herb/berry only; strawberry+
  basil v2 with categories preserved; leach values in (0,1]; arithmetic 20→38 and 42→45 sound;
  blueberry pollination_partners_required=1 valid (integer column, check >= 0).
- Vision: ALIGNED — identical approved 0062 pattern + a net-new enrichment-guard test;
  citations spot-verified (RHS/UW/Illinois/TAMU rosemary; UMN/RHS/Clemson/UMD blueberry).
  Ship as-is.

## 0064-catalog-batch3 (2026-06-11)
- Guardian: PASS — 15 tuples match BATCH3_IDS; passion fruit + tomatillo v2 'fruit';
  tomatillo requires_support=true + pollination_partners_required=2 preserved through the
  upsert SET list; leach values in (0,1]; arithmetic 38→51, 45→48 sound.
- Vision: ALIGNED — tomatillo pollination round-trip verified (SQL + JSON + test assert);
  citations spot-verified (citrus: RHS/Clemson/UF-IFAS et al; aloe: multi-institution).
  Ship as-is.

## 0064 verification + 0065-catalog-batch4 (2026-06-11)
- 0064 DEVIATION ACCEPTED (0050 precedent): pre-existing advisories-api test pinned passion
  fruit idealMaxLiters 190; the cited enrichment (RHS/UF-IFAS) sets 95–150; impl updated the
  stale literal + provenance comment, attachment untouched, flagged clearly. Planner verified
  150 IS the cited value. Lesson folded into 0065 gates: value-pin sweep for enriched rows.
- 0065 Guardian: PASS — 24 tuples; 9 houseplant + 15 ornamental; category math across all 4
  batches = the §7 breakdown exactly (75); value-pin sweep clean (only schema-shape fixture
  references pothos, DB-independent); arithmetic 51→75, 48→52.
- 0065 Vision: ALIGNED — houseplant cadences consumer-sane (monstera 7d conservative w/
  confidence note; pansy 3d source-backed); Gate B count=9 exact. Ship as-is.
